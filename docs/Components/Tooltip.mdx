---
title: Tooltip
pagination_next: null
pagination_prev: null
---

import { Tooltip } from '../../src/components/Tooltip'
import Codeblock from './utils/Codeblock'
import { StyledLabel } from '../Guidelines/utils/Guidelines.styles'

<hr/>

<p>Tooltip provides on-demand, contextual information to users as they hover over or focus on elements, enhancing understanding and interaction without cluttering the interface.</p>

<hr/>

## Top
<Codeblock isCenter={true} codeString={`<Tooltip text="This is the tooltip content!" position="top">
    <label>Hover Me</label>
</Tooltip>`}>
  <Tooltip text="This is the tooltip content!" position="top">
    <StyledLabel>Hover Me</StyledLabel>
  </Tooltip>
</Codeblock>


<br/>

## Bottom
<Codeblock isCenter={true} codeString={`<Tooltip text="This is the tooltip content!" position="bottom">
    <label>Hover Me</label>
</Tooltip>`}>
  <Tooltip text="This is the tooltip content!" position="bottom">
    <StyledLabel>Hover Me</StyledLabel>
  </Tooltip>
</Codeblock>

<br/>

## Left
<Codeblock isCenter={true} codeString={`<Tooltip text="This is the tooltip content!" position="left">
    <label>Hover Me</label>
</Tooltip>`}>
  <Tooltip text="This is the tooltip content!" position="left">
    <StyledLabel>Hover Me</StyledLabel>
  </Tooltip>
</Codeblock>

<br/>

## Right
<Codeblock isCenter={true} codeString={`<Tooltip text="This is the tooltip content!" position="right">
    <label>Hover Me</label>
</Tooltip>`}>
  <Tooltip text="This is the tooltip content!" position="right">
    <StyledLabel>Hover Me</StyledLabel>
  </Tooltip>
</Codeblock>

## Open
<Codeblock isCenter={true} codeString={`<Tooltip text="This is the tooltip content!" setOpen={true}>
    <label>Hover Me</label>
</Tooltip>`}>
  <Tooltip text="This is the tooltip content!" setOpen={true}>
    <StyledLabel>Hover Me</StyledLabel>
  </Tooltip>
</Codeblock>

## Dark
<Codeblock isCenter={true} codeString={`<Tooltip text="This is the tooltip content!" isDark={true}>
    <label>Hover Me</label>
</Tooltip>`}>
  <Tooltip text="This is the tooltip content!" isDark={true}>
    <StyledLabel>Hover Me</StyledLabel>
  </Tooltip>
</Codeblock>

## Hidden pointer
<Codeblock isCenter={true} codeString={`<Tooltip text="This is the tooltip content!" hidePointer={true}>
    <label>Hover Me</label>
</Tooltip>`}>
  <Tooltip text="This is the tooltip content!" hidePointer={true}>
    <StyledLabel>Hover Me</StyledLabel>
  </Tooltip>
</Codeblock>
